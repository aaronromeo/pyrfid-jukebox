# RFID-Music

## Getting Started

### Prerequisites
_Assumes `cmus` is installed_

* Create a venv
    * `python -m venv env`
* Install the `requirements.txt`
    * `pip install -r requirements.txt`




